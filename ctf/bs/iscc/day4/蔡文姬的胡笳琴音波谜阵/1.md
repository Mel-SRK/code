我会先把 APK 的基本信息和内部结构摸出来，再看是静态提取、反编译、还是跑动态校验更合适。移动题通常线索藏在资源、DEX、native so 或签名校验里，我先从低成本路径切进去。

已运行 2 条命令

这个包 103MB，体量像是带了 Flutter/Unity/native 资源，不一定只是一个 Java 校验。下一步我先按 zip 结构统计文件类型和可疑名字，确定主战场在哪。

已运行 3 条命令

Reconnecting... 1/5



看到 libmobile04.so 和 assets/appview_cache.db，这题大概率有 native + SQLCipher 的组合；同时那个 77MB 的 res/6V.mp4 也值得扫一下是否藏东西。我先把内容解到工作区，只读分析，不动原 APK。



已运行 $apk='C:\Users\21746\Desktop\iscc\区域\二次\mob\app-release\app-release\app-release.apk'; $out='C:\Users\21746\Documents\Codex\2026-05-13\files-mentioned-by-the-user-app\apk_extract'; if (-not (Test-Path -LiteralPath $out)) { New-Item -ItemType Directory -Path $out | Out-Null }; Add-Type -AssemblyName System.IO.Compression.FileSystem; [System.IO.Compression.ZipFile]::ExtractToDirectory($apk, $out, $true); Get-ChildItem -LiteralPath $out -Force | Select-Object Name,Length,Mode



Reconnecting... 1/5



Reconnecting... 2/5



提取函数的重载在这个 PowerShell/.NET 组合里不接受 overwrite 参数，先换成逐条 entry 解压并做路径约束。小坑不影响方向。



已运行 $apk='C:\Users\21746\Desktop\iscc\区域\二次\mob\app-release\app-release\app-release.apk'; $out='C:\Users\21746\Documents\Codex\2026-05-13\files-mentioned-by-the-user-app\apk_extract'; Add-Type -AssemblyName System.IO.Compression.FileSystem; if (-not (Test-Path -LiteralPath $out)) { New-Item -ItemType Directory -Path $out | Out-Null }; $outFull=[IO.Path]::GetFullPath($out); if (-not $outFull.EndsWith([IO.Path]::DirectorySeparatorChar)) { $outFull += [IO.Path]::DirectorySeparatorChar }; $zip=[System.IO.Compression.ZipFile]::OpenRead($apk); foreach($entry in $zip.Entries){ $dest=[IO.Path]::GetFullPath([IO.Path]::Combine($outFull,$entry.FullName)); if (-not $dest.StartsWith($outFull,[StringComparison]::OrdinalIgnoreCase)) { throw "Unsafe zip entry: $($entry.FullName)" }; if ($entry.FullName.EndsWith('/')) { [IO.Directory]::CreateDirectory($dest) | Out-Null; continue }; [IO.Directory]::CreateDirectory([IO.Path]::GetDirectoryName($dest)) | Out-Null; $in=$entry.Open(); try { $outStream=[IO.File]::Open($dest,[IO.FileMode]::Create,[IO.FileAccess]::Write); try { $in.CopyTo($outStream) } finally { $outStream.Dispose() } } finally { $in.Dispose() } }; $zip.Dispose(); Get-ChildItem -LiteralPath $out -Force | Select-Object Name,Length,Mode



现在我开始扫字符串和入口名。先不急着反编译全量代码，CTF APK 往往会把关键字、native 函数名、数据库 key 或资源名直接暴露在常量区。

已运行 3 条命令

rg 在这个桌面沙箱里被系统拒绝执行了，我换成 Python/PowerShell 做二进制字符串提取。接下来重点看 classes.dex、classes2.dex、libmobile04.so 和那个缓存数据库。

已运行 3 条命令

Reconnecting... 1/5



classes.dex 里已经直接出现了 ISCC{，这是好信号，但它可能只是 UI 拼接前缀。我要把 com/example/mobile04 的类和方法定位出来，再追它怎么调用 native 函数。

已运行 3 条命令

Reconnecting... 1/5



刚才枚举 DEX 字符串时撞到了 Windows 控制台编码，我会把输出改成安全编码。现在目标很明确：先把 com.example.mobile04 自己写的类剥出来，避开一大坨 AndroidX/Kotlin 噪声。

已运行 3 条命令

我会写一个很小的 DEX 解析器，把目标包的类、方法、字段和字节码调用关系打印出来。这里不用完整反编译，先抓常量、native 调用和 SQLCipher 初始化路径，速度会更快。



已运行 @' import struct, pathlib, sys sys.stdout.reconfigure(encoding='utf-8', errors='backslashreplace') DEX=pathlib.Path(r'C:\Users\21746\Documents\Codex\2026-05-13\files-mentioned-by-the-user-app\apk_extract\classes.dex') data=DEX.read_bytes() def uleb(pos): res=0; shift=0 while True: b=data[pos]; pos+=1 res |= (b & 0x7f) << shift if b < 0x80: break shift += 7 return res,pos def sleb(pos): res=0; shift=0; size=32 while True: b=data[pos]; pos+=1 res |= (b & 0x7f) << shift shift += 7 if b < 0x80: break if shift < size and (b & 0x40): res |= - (1 << shift) return res,pos string_ids_size,string_ids_off=struct.unpack_from('<II',data,0x38) type_ids_size,type_ids_off=struct.unpack_from('<II',data,0x40) proto_ids_size,proto_ids_off=struct.unpack_from('<II',data,0x48) field_ids_size,field_ids_off=struct.unpack_from('<II',data,0x50) method_ids_size,method_ids_off=struct.unpack_from('<II',data,0x58) class_defs_size,class_defs_off=struct.unpack_from('<II',data,0x60) strings=[] for i in range(string_ids_size): off=struct.unpack_from('<I',data,string_ids_off+i*4)[0] _,pos=uleb(off) end=data.index(b'\x00',pos) strings.append(data[pos:end].decode('utf-8','backslashreplace')) types=[] for i in range(type_ids_size): idx=struct.unpack_from('<I',data,type_ids_off+i*4)[0] types.append(strings[idx]) protos=[] for i in range(proto_ids_size): shorty_idx,ret_idx,params_off=struct.unpack_from('<III',data,proto_ids_off+i*12) params=[] if params_off: sz=struct.unpack_from('<I',data,params_off)[0] params=[types[struct.unpack_from('<H',data,params_off+4+2*j)[0]] for j in range(sz)] protos.append((strings[shorty_idx],types[ret_idx],params)) fields=[] for i in range(field_ids_size): class_idx,type_idx,name_idx=struct.unpack_from('<HHI',data,field_ids_off+i*8) fields.append((types[class_idx],types[type_idx],strings[name_idx])) methods=[] for i in range(method_ids_size): class_idx,proto_idx,name_idx=struct.unpack_from('<HHI',data,method_ids_off+i*8) cls=types[class_idx]; name=strings[name_idx]; shorty,ret,params=protos[proto_idx] methods.append((cls,name,ret,params,shorty)) # instruction metadata: (name, code units, special) # For variable formats, determine size manually. def instr_size(insns, i): w=insns[i]; op=w & 0xff if op in (0x00,): # nop or packed/sparse/fill payload signatures hi=w>>8 if hi==0x01: # packed-switch-payload size=insns[i+1]; return 4 + size*2 if hi==0x02: # sparse-switch-payload size=insns[i+1]; return 2 + size*4 if hi==0x03: # fill-array-data-payload elem_width=insns[i+1]; size=insns[i+2] | (insns[i+3]<<16); return 4 + ((size*elem_width + 1)//2) return 1 # 1 code unit formats if op in set([0x01,0x04,0x07,0x0a,0x0b,0x0c,0x0d,0x0e,0x0f,0x10,0x11,0x12,0x1d,0x1e,0x27,0x28,0x29,0x2a]) or (0x7b <= op <= 0x8f) or (0xb0 <= op <= 0xcf): # note goto/16, goto/32 are 2/3 not caught by op 0x29/0x2a above but included wrong; fix below pass one={0x00,0x01,0x04,0x07,0x0a,0x0b,0x0c,0x0d,0x0e,0x0f,0x10,0x11,0x12,0x1d,0x1e,0x27,0x28} two=set([0x02,0x05,0x08,0x13,0x15,0x16,0x19,0x1a,0x1c,0x1f,0x20,0x21,0x22,0x23,0x24,0x29,0x2b,0x2c])|set(range(0x2d,0x3e))|set(range(0x44,0x52))|set(range(0x52,0x60))|set(range(0x60,0x6e))|set(range(0x90,0xb0))|set(range(0xd0,0xe3)) three=set([0x03,0x06,0x09,0x14,0x17,0x18,0x1b,0x25,0x26,0x2a])|set(range(0x3e,0x44))|set(range(0x6e,0x79))|set([0xfa,0xfb,0xfc,0xfd,0xfe,0xff]) five=set([0x24]) if op in one: return 1 if op in two: return 2 if op in three: return 3 # invoke-polymorphic/range and const-method-handle/type maybe 0xfa... uncertain return 1 opnames={ 0x00:'nop',0x01:'move',0x02:'move/from16',0x03:'move/16',0x04:'move-wide',0x05:'move-wide/from16',0x06:'move-wide/16',0x07:'move-object',0x08:'move-object/from16',0x09:'move-object/16',0x0a:'move-result',0x0b:'move-result-wide',0x0c:'move-result-object',0x0d:'move-exception',0x0e:'return-void',0x0f:'return',0x10:'return-wide',0x11:'return-object',0x12:'const/4',0x13:'const/16',0x14:'const',0x15:'const/high16',0x16:'const-wide/16',0x17:'const-wide/32',0x18:'const-wide',0x19:'const-wide/high16',0x1a:'const-string',0x1b:'const-string/jumbo',0x1c:'const-class',0x1d:'monitor-enter',0x1e:'monitor-exit',0x1f:'check-cast',0x20:'instance-of',0x21:'array-length',0x22:'new-instance',0x23:'new-array',0x24:'filled-new-array',0x25:'filled-new-array/range',0x26:'fill-array-data',0x27:'throw',0x28:'goto',0x29:'goto/16',0x2a:'goto/32',0x2b:'packed-switch',0x2c:'sparse-switch'} for base,names in [(0x2d,['cmpl-float','cmpg-float','cmpl-double','cmpg-double','cmp-long']),(0x32,['if-eq','if-ne','if-lt','if-ge','if-gt','if-le']),(0x38,['if-eqz','if-nez','if-ltz','if-gez','if-gtz','if-lez'])]: for j,n in enumerate(names): opnames[base+j]=n for base,prefix in [(0x44,'aget'),(0x4b,'aput')]: suff=['','-wide','-object','-boolean','-byte','-char','-short'] for j,s in enumerate(suff): opnames[base+j]=prefix+s for base,prefix in [(0x52,'iget'),(0x59,'iput'),(0x60,'sget'),(0x67,'sput')]: suff=['','-wide','-object','-boolean','-byte','-char','-short'] for j,s in enumerate(suff): opnames[base+j]=prefix+s for j,n in enumerate(['invoke-virtual','invoke-super','invoke-direct','invoke-static','invoke-interface','invoke-virtual/range','invoke-super/range','invoke-direct/range','invoke-static/range','invoke-interface/range']): opnames[0x6e+j]=n for j,n in enumerate(['neg-int','not-int','neg-long','not-long','neg-float','neg-double','int-to-long','int-to-float','int-to-double','long-to-int','long-to-float','long-to-double','float-to-int','float-to-long','float-to-double','double-to-int','double-to-long','double-to-float','int-to-byte','int-to-char','int-to-short']): opnames[0x7b+j]=n binops=['add','sub','mul','div','rem','and','or','xor','shl','shr','ushr'] typesuf=['int','long','float','double'] op=0x90 for t in typesuf: for b in binops[:(11 if t in ['int','long'] else 5)]: opnames[op]=f'{b}-{t}'; op+=1 for b in binops: opnames[0xb0+binops.index(b)]=f'{b}-int/2addr' # not complete but enough for j,n in enumerate(['add-int/lit16','rsub-int','mul-int/lit16','div-int/lit16','rem-int/lit16','and-int/lit16','or-int/lit16','xor-int/lit16']): opnames[0xd0+j]=n for j,n in enumerate(['add-int/lit8','rsub-int/lit8','mul-int/lit8','div-int/lit8','rem-int/lit8','and-int/lit8','or-int/lit8','xor-int/lit8','shl-int/lit8','shr-int/lit8','ushr-int/lit8']): opnames[0xd8+j]=n def s16(x): return x-0x10000 if x&0x8000 else x def s8(x): return x-0x100 if x&0x80 else x def decode(insns, i): w=insns[i]; op=w&0xff; aa=(w>>8)&0xff; name=opnames.get(op,f'op_{op:02x}') size=instr_size(insns,i) extra='' # references if op in (0x1a,): idx=insns[i+1]; extra=f'v{aa}, string@{idx} {strings[idx]!r}' elif op==0x1b: idx=insns[i+1] | (insns[i+2]<<16); extra=f'v{aa}, string@{idx} {strings[idx]!r}' elif op in (0x1c,0x1f,0x20,0x22,0x23,0x24,0x25): idx=insns[i+1]; extra=f'type@{idx} {types[idx] if idx < len(types) else "?"}' elif 0x52 <= op <= 0x6d: idx=insns[i+1] extra=f'field@{idx} {fields[idx] if idx < len(fields) else "?"}' elif 0x6e <= op <= 0x78: idx=insns[i+1] m=methods[idx] if idx < len(methods) else None if m: cls,n,ret,params,shorty=m; extra=f'method@{idx} {cls}->{n}({", ".join(params)}):{ret}' else: extra=f'method@{idx}' elif op in (0x12,): lit=(w>>12)&0xf; lit=lit-16 if lit&8 else lit; extra=f'v{(w>>8)&0xf}, #{lit}' elif op in (0x13,0x15,0x16,0x19): extra=f'v{aa}, #{s16(insns[i+1])}' elif op==0x14: val=insns[i+1] | (insns[i+2]<<16); extra=f'v{aa}, #{val}' elif op in range(0x32,0x3e): extra=f'target {i+s16(insns[i+1])}' elif op==0x28: off=s8(aa); extra=f'target {i+off}' elif op==0x29: extra=f'target {i+s16(insns[i+1])}' elif op==0x2a: off=insns[i+1] | (insns[i+2]<<16); extra=f'target {i+off}' return name, size, extra def parse_class_data(off): pos=off sf,pos=uleb(pos); inf,pos=uleb(pos); dm,pos=uleb(pos); vm,pos=uleb(pos) fields_out=[]; midx=0 for _ in range(sf+inf): diff,pos=uleb(pos); acc,pos=uleb(pos); midx+=diff; fields_out.append((midx,acc)) methods_out=[]; midx=0 for _ in range(dm+vm): diff,pos=uleb(pos); acc,pos=uleb(pos); code_off,pos=uleb(pos); midx+=diff; methods_out.append((midx,acc,code_off)) return fields_out, methods_out print('TARGET METHODS') for ci in range(class_defs_size): off=class_defs_off+ci*32 class_idx,access,super_idx,interfaces_off,source_file_idx,annotations_off,class_data_off,static_values_off=struct.unpack_from('<IIIIIIII',data,off) desc=types[class_idx] if not desc.startswith('Lcom/example/mobile04/'): continue print('\nCLASS', desc) if not class_data_off: continue flds, mths=parse_class_data(class_data_off) for idx,acc in flds: print(' FIELD', idx, fields[idx], 'acc', hex(acc)) for midx,acc,code_off in mths: cls,name,ret,params,shorty=methods[midx] print(f' METHOD #{midx} acc={hex(acc)} code={hex(code_off)} {name}({", ".join(params)}):{ret}') if code_off: regs,ins,outs,tries,debug,insns_size=struct.unpack_from('<HHHHII',data,code_off) insns=list(struct.unpack_from('<'+'H'*insns_size,data,code_off+16)) print(f' regs={regs} ins={ins} outs={outs} insns={insns_size}') i=0; shown=0 while i < len(insns) and shown < 300: name2,size,extra=decode(insns,i) # print interesting, and all for target classes maybe not too many print(f' {i:04x}: {name2:<24} {extra}') i += max(size,1); shown += 1 if i < len(insns): print(' ...') '@ | python - > dex_mobile04_disasm.txt; Get-Item -LiteralPath dex_mobile04_disasm.txt | Select-Object FullName,Length



Reconnecting... 1/5



Reconnecting... 2/5



Reconnecting... 3/5



我写的快速解析器在某条指令长度上算错了，输出只落了一小段。接下来我把它收窄到“只打印方法签名、字符串引用、方法调用和字段访问”，这样更稳，也更贴近解题需要。

已运行 2 条命令

我已经避开了完整工具链缺失的问题，直接用 DEX 格式取目标包的信息。下一步会只提取“字符串常量、调用目标、字段访问”，这基本就是反编译器在这题里最有价值的部分。



已运行 @' import struct, pathlib, sys sys.stdout.reconfigure(encoding='utf-8', errors='backslashreplace') path=pathlib.Path(r'C:\Users\21746\Documents\Codex\2026-05-13\files-mentioned-by-the-user-app\apk_extract\classes.dex') data=path.read_bytes() def uleb(pos): res=0; shift=0 while True: b=data[pos]; pos+=1 res |= (b & 0x7f) << shift if b < 0x80: break shift += 7 return res,pos def read_string(off): _,pos=uleb(off) bs=[] while data[pos]!=0: bs.append(data[pos]); pos+=1 return bytes(bs).decode('utf-8','backslashreplace') string_ids_size,string_ids_off=struct.unpack_from('<II',data,0x38) type_ids_size,type_ids_off=struct.unpack_from('<II',data,0x40) proto_ids_size,proto_ids_off=struct.unpack_from('<II',data,0x48) field_ids_size,field_ids_off=struct.unpack_from('<II',data,0x50) method_ids_size,method_ids_off=struct.unpack_from('<II',data,0x58) class_defs_size,class_defs_off=struct.unpack_from('<II',data,0x60) strings=[read_string(struct.unpack_from('<I',data,string_ids_off+i*4)[0]) for i in range(string_ids_size)] types=[strings[struct.unpack_from('<I',data,type_ids_off+i*4)[0]] for i in range(type_ids_size)] protos=[] for i in range(proto_ids_size): shorty_idx,ret_idx,params_off=struct.unpack_from('<III',data,proto_ids_off+i*12) params=[] if params_off: sz=struct.unpack_from('<I',data,params_off)[0] params=[types[struct.unpack_from('<H',data,params_off+4+2*j)[0]] for j in range(sz)] protos.append((strings[shorty_idx], types[ret_idx], params)) fields=[] for i in range(field_ids_size): c,t,n=struct.unpack_from('<HHI',data,field_ids_off+i*8) fields.append(f'{types[c]}->{strings[n]}:{types[t]}') methods=[] for i in range(method_ids_size): c,p,n=struct.unpack_from('<HHI',data,method_ids_off+i*8) shorty,ret,params=protos[p] methods.append(f'{types[c]}->{strings[n]}({",".join(params)}):{ret}') def parse_class_data(off): pos=off sf,pos=uleb(pos); inf,pos=uleb(pos); dm,pos=uleb(pos); vm,pos=uleb(pos) f=[]; idx=0 for _ in range(sf+inf): d,pos=uleb(pos); acc,pos=uleb(pos); idx+=d; f.append((idx,acc)) m=[]; idx=0 for _ in range(dm+vm): d,pos=uleb(pos); acc,pos=uleb(pos); co,pos=uleb(pos); idx+=d; m.append((idx,acc,co)) return f,m sizes={} for op in range(256): sizes[op]=None for op in [0x00,0x01,0x04,0x07,0x0a,0x0b,0x0c,0x0d,0x0e,0x0f,0x10,0x11,0x12,0x1d,0x1e,0x21,0x27,0x28]+list(range(0x7b,0x90))+list(range(0xb0,0xd0)): sizes[op]=1 for op in [0x02,0x05,0x08,0x13,0x15,0x16,0x19,0x1a,0x1c,0x1f,0x20,0x22,0x23,0x29,0x2d,0x2e,0x2f,0x30,0x31]+list(range(0x32,0x3e))+list(range(0x44,0x6e))+list(range(0xd0,0xe3))+[0xfc,0xfd]: sizes[op]=2 for op in [0x03,0x06,0x09,0x14,0x17,0x1b,0x24,0x25,0x26,0x2a,0x2b,0x2c]+list(range(0x6e,0x73))+list(range(0x74,0x79))+[0xfe,0xff]: sizes[op]=3 sizes[0x18]=5 sizes[0xfa]=4; sizes[0xfb]=4 def instr_size(insns,i): if i>=len(insns): return 1 w=insns[i]; op=w&0xff if op==0x00: hi=w>>8 if hi==1 and i+1<len(insns): return 4+insns[i+1]*2 if hi==2 and i+1<len(insns): return 2+insns[i+1]*4 if hi==3 and i+3<len(insns): elem=insns[i+1]; n=insns[i+2]|(insns[i+3]<<16) return 4+((elem*n+1)//2) return sizes.get(op) or 1 def s16(x): return x-0x10000 if x&0x8000 else x interesting_ops=set([0x1a,0x1b,0x1c,0x1f,0x20,0x22,0x23,0x24,0x25])|set(range(0x52,0x6e))|set(range(0x6e,0x79))|set([0x12,0x13,0x14,0x15,0x16,0x17,0x18,0x19,0x26,0x2b,0x2c]) for ci in range(class_defs_size): off=class_defs_off+ci*32 class_idx,acc,super_idx,interfaces_off,source_idx,ann_off,class_data_off,static_values_off=struct.unpack_from('<IIIIIIII',data,off) cls=types[class_idx] if not cls.startswith('Lcom/example/mobile04/'): continue src='' if source_idx==0xffffffff else strings[source_idx] print('\nCLASS', cls, 'super', types[super_idx] if super_idx < len(types) else '?', 'src', src, 'acc', hex(acc)) if not class_data_off: continue flds,mths=parse_class_data(class_data_off) for idx,a in flds: print(' FIELD', hex(a), fields[idx]) for midx,a,co in mths: print(' METHOD', hex(a), methods[midx], 'code', hex(co)) if not co: continue regs,ins,outs,tries,debug,insns_size=struct.unpack_from('<HHHHII',data,co) insns=list(struct.unpack_from('<'+'H'*insns_size,data,co+16)) print(' regs/ins/outs/size', regs,ins,outs,insns_size) i=0; count=0 while i < len(insns): w=insns[i]; op=w&0xff; aa=(w>>8)&0xff desc=None try: if op==0x1a and i+1<len(insns): idx=insns[i+1]; desc=f'{i:04x} const-string v{aa}, {strings[idx]!r}' elif op==0x1b and i+2<len(insns): idx=insns[i+1]|(insns[i+2]<<16); desc=f'{i:04x} const-string/jumbo v{aa}, {strings[idx]!r}' elif op in (0x1c,0x1f,0x20,0x22,0x23,0x24,0x25) and i+1<len(insns): idx=insns[i+1]; desc=f'{i:04x} type-ref op{op:02x} {types[idx] if idx<len(types) else idx}' elif 0x52 <= op <= 0x6d and i+1<len(insns): idx=insns[i+1]; desc=f'{i:04x} field-ref op{op:02x} {fields[idx] if idx<len(fields) else idx}' elif 0x6e <= op <= 0x78 and i+1<len(insns): idx=insns[i+1]; desc=f'{i:04x} invoke op{op:02x} {methods[idx] if idx<len(methods) else idx}' elif op in (0x12,): lit=(w>>12)&0xf; lit=lit-16 if lit&8 else lit; desc=f'{i:04x} const/4 #{lit}' elif op in (0x13,0x15,0x16,0x19) and i+1<len(insns): desc=f'{i:04x} const op{op:02x} #{s16(insns[i+1])}' elif op in (0x14,0x17) and i+2<len(insns): val=insns[i+1]|(insns[i+2]<<16); desc=f'{i:04x} const op{op:02x} #{val}' elif op==0x18 and i+4<len(insns): val=insns[i+1]|(insns[i+2]<<16)|(insns[i+3]<<32)|(insns[i+4]<<48); desc=f'{i:04x} const-wide #{val}' elif op in (0x26,0x2b,0x2c) and i+2<len(insns): offv=insns[i+1]|(insns[i+2]<<16); desc=f'{i:04x} payload/switch target {i+offv}' except Exception as e: desc=f'{i:04x} decode-error op{op:02x} {e}' if desc: print(' ', desc); count += 1 i += instr_size(insns,i) if count==0: print(' (no interesting refs)') '@ | python -



Reconnecting... 1/5



Reconnecting... 2/5



解析器第二版还是在边缘指令上绊了一下，但已经确认了自定义包只有几十个类，不复杂。我接下来改成“方法级摘要写文件”，出错跳过单条指令，不让一个异常挡住整题。



已运行 @' import struct, pathlib, sys, traceback sys.stdout.reconfigure(encoding='utf-8', errors='backslashreplace') path=pathlib.Path(r'C:\Users\21746\Documents\Codex\2026-05-13\files-mentioned-by-the-user-app\apk_extract\classes.dex') data=path.read_bytes() def uleb(pos): res=0; shift=0 while True: b=data[pos]; pos+=1 res |= (b & 0x7f) << shift if b < 0x80: break shift += 7 return res,pos def read_string(off): _,pos=uleb(off) end=data.index(b'\x00',pos) return data[pos:end].decode('utf-8','backslashreplace') header=lambda off: struct.unpack_from('<II',data,off) string_ids_size,string_ids_off=header(0x38); type_ids_size,type_ids_off=header(0x40); proto_ids_size,proto_ids_off=header(0x48); field_ids_size,field_ids_off=header(0x50); method_ids_size,method_ids_off=header(0x58); class_defs_size,class_defs_off=header(0x60) strings=[read_string(struct.unpack_from('<I',data,string_ids_off+i*4)[0]) for i in range(string_ids_size)] types=[strings[struct.unpack_from('<I',data,type_ids_off+i*4)[0]] for i in range(type_ids_size)] protos=[] for i in range(proto_ids_size): shorty_idx,ret_idx,params_off=struct.unpack_from('<III',data,proto_ids_off+i*12) params=[] if params_off: sz=struct.unpack_from('<I',data,params_off)[0] params=[types[struct.unpack_from('<H',data,params_off+4+2*j)[0]] for j in range(sz)] protos.append((strings[shorty_idx],types[ret_idx],params)) fields=[] for i in range(field_ids_size): c,t,n=struct.unpack_from('<HHI',data,field_ids_off+i*8); fields.append(f'{types[c]}->{strings[n]}:{types[t]}') methods=[] for i in range(method_ids_size): c,p,n=struct.unpack_from('<HHI',data,method_ids_off+i*8); shorty,ret,params=protos[p]; methods.append(f'{types[c]}->{strings[n]}({",".join(params)}):{ret}') def parse_class_data(off): pos=off sf,pos=uleb(pos); inf,pos=uleb(pos); dm,pos=uleb(pos); vm,pos=uleb(pos) f=[]; idx=0 for _ in range(sf+inf): d,pos=uleb(pos); acc,pos=uleb(pos); idx+=d; f.append((idx,acc)) m=[]; idx=0 for _ in range(dm+vm): d,pos=uleb(pos); acc,pos=uleb(pos); co,pos=uleb(pos); idx+=d; m.append((idx,acc,co)) return f,m sizes={op:1 for op in range(256)} # conservative approximate: all variable/ref instructions we care about are at least enough to avoid infinite loops for op in [0x02,0x05,0x08,0x13,0x15,0x16,0x19,0x1a,0x1c,0x1f,0x20,0x22,0x23,0x29,0x2d,0x2e,0x2f,0x30,0x31]+list(range(0x32,0x3e))+list(range(0x44,0x6e))+list(range(0xd0,0xe3))+[0xfc,0xfd]: sizes[op]=2 for op in [0x03,0x06,0x09,0x14,0x17,0x1b,0x24,0x25,0x26,0x2a,0x2b,0x2c]+list(range(0x6e,0x73))+list(range(0x74,0x79))+[0xfe,0xff]: sizes[op]=3 sizes[0x18]=5; sizes[0xfa]=4; sizes[0xfb]=4 def instr_size(insns,i): w=insns[i]; op=w&0xff if op==0x00: hi=w>>8 try: if hi==1: return 4+insns[i+1]*2 if hi==2: return 2+insns[i+1]*4 if hi==3: elem=insns[i+1]; n=insns[i+2]|(insns[i+3]<<16); return 4+((elem*n+1)//2) except Exception: return 1 return sizes.get(op,1) def s16(x): return x-0x10000 if x&0x8000 else x out=[] for ci in range(class_defs_size): cdo=class_defs_off+ci*32 class_idx,acc,super_idx,interfaces_off,source_idx,ann_off,class_data_off,static_values_off=struct.unpack_from('<IIIIIIII',data,cdo) cls=types[class_idx] if not cls.startswith('Lcom/example/mobile04/'): continue src='' if source_idx==0xffffffff else strings[source_idx] out.append(f'\nCLASS {cls} super={types[super_idx] if super_idx<len(types) else "?"} src={src} acc={hex(acc)}') if not class_data_off: continue flds,mths=parse_class_data(class_data_off) for idx,a in flds: out.append(f' FIELD {hex(a)} {fields[idx]}') for midx,a,co in mths: out.append(f' METHOD {hex(a)} #{midx} {methods[midx]} code={hex(co)}') if not co: continue try: regs,ins,outs_,tries,debug,insns_size=struct.unpack_from('<HHHHII',data,co) insns=list(struct.unpack_from('<'+'H'*insns_size,data,co+16)) out.append(f' regs={regs} ins={ins} outs={outs_} insns={insns_size}') i=0 while i < len(insns): w=insns[i]; op=w&0xff; aa=(w>>8)&0xff desc=None try: if op==0x1a and i+1<len(insns): desc=f'{i:04x} const-string {strings[insns[i+1]]!r}' elif op==0x1b and i+2<len(insns): desc=f'{i:04x} const-string/jumbo {strings[insns[i+1]|(insns[i+2]<<16)]!r}' elif op in (0x1c,0x1f,0x20,0x22,0x23,0x24,0x25) and i+1<len(insns): idx=insns[i+1]; desc=f'{i:04x} type op{op:02x} {types[idx] if 0<=idx<len(types) else idx}' elif 0x52 <= op <= 0x6d and i+1<len(insns): idx=insns[i+1]; desc=f'{i:04x} field op{op:02x} {fields[idx] if 0<=idx<len(fields) else idx}' elif 0x6e <= op <= 0x78 and i+1<len(insns): idx=insns[i+1]; desc=f'{i:04x} invoke op{op:02x} {methods[idx] if 0<=idx<len(methods) else idx}' elif op==0x12: lit=(w>>12)&0xf; lit=lit-16 if lit&8 else lit; desc=f'{i:04x} const/4 {lit}' elif op in (0x13,0x15,0x16,0x19) and i+1<len(insns): desc=f'{i:04x} const op{op:02x} {s16(insns[i+1])}' elif op in (0x14,0x17) and i+2<len(insns): desc=f'{i:04x} const op{op:02x} {insns[i+1]|(insns[i+2]<<16)}' elif op==0x18 and i+4<len(insns): desc=f'{i:04x} const-wide {insns[i+1]|(insns[i+2]<<16)|(insns[i+3]<<32)|(insns[i+4]<<48)}' elif op in (0x26,0x2b,0x2c) and i+2<len(insns): desc=f'{i:04x} payload/switch target+{insns[i+1]|(insns[i+2]<<16)}' except Exception as e: desc=f'{i:04x} decode-error op{op:02x} {type(e).__name__}:{e}' if desc: out.append(' '+desc) i += max(1,instr_size(insns,i)) except Exception as e: out.append(f' METHOD-DECODE-ERROR {type(e).__name__}:{e}') outp=pathlib.Path(r'C:\Users\21746\Documents\Codex\2026-05-13\files-mentioned-by-the-user-app\mobile04_refs.txt') outp.write_text('\n'.join(out),encoding='utf-8') print(outp, len(out), 'lines') '@ | python -



我把脚本改成更保守时仍然在某个类/索引上出错，说明需要先把异常位置定位清楚，而不是继续盲调。现在我会检查本机是否已有逆向/二进制库，能省很多时间；没有的话再走自写解析器。

已运行 2 条命令

好，机器上其实有 androguard、apktool 和 NDK 的 llvm-objdump，这下不用硬啃 DEX 了。我会用成熟工具把 smali/native 都拆出来，然后回到算法本身。

已运行 3 条命令

apktool 已经成功拆出 smali 了；androguard 只是缺 cryptography，暂时不需要装。现在我直接读 apktool_out/smali/com/example/mobile04，先把 Java 层的流程还原出来。

已运行 3 条命令

我先把噪声输出收一下，直接看几个核心 smali 文件：MainActivity、短名算法类、以及和数据库/native 相关的调用点。这里通常 Java 层会告诉我们输入如何拆分、native 层负责哪一段。

已运行 4 条命令

Java 层已经能读出一半逻辑：输入要形如 ISCC{...}，同时还要求先点 8 个图标形成一段 25 到 64 字符的“旋律序列”。我继续看 P1/K1/Z1/HarpMelodyManager，把前缀、后缀、native 注册名和序列编码规则拼起来。

已运行 5 条命令

现在 Java 层的形状已经清楚了：真正的校验在 Z1.l(path, cw, inner)，而 cw 是由点击按钮形成的序列经过 FNV64 + TEA 得到的 SQLCipher 风格口令。接下来我要拆 native 的两个函数：d(int) 负责生成 TEA key，l(...) 负责最终验 flag。

已运行 4 条命令

nm 说普通符号表被剥了，但字符串里还有 JNI/native 方法名，说明动态符号或注册表仍然能定位。我要改用 llvm-readobj --dyn-symbols 和按字符串地址交叉引用，先找 _n_impl/_n_derive_impl 的真实入口。

已运行 4 条命令

native 里动态符号还保留了：_f3、_f4、_n_impl、_n_derive_impl、JNI_OnLoad 都有地址。接下来我直接按地址反汇编这几个函数，不看整库，开始还原最终公式。

已运行 3 条命令

_f4 看起来是 Base58 解码，_C1::_v1 是 RC6-32/20 轮块加密。现在只剩最后一步：看 _n_impl 是拿 cw + cache.dat 前16字节 派生什么，再和 Base58 解出的 16 字节怎么比较。

已运行 4 条命令

Java 部分还有一个关键约束：inner 必须是 32 字节，所以 flag 是 ISCC{32位内容}。我正在把 native 的 _n_impl 中段拆出来，这段会告诉我们 32 位内容是 Base58、CRC 还是 RC6 密文。

已运行 4 条命令

我已经把 _n_impl 主要逻辑还原出来了：它用 cw + cache.dat前16字节 做 CRC32，当作解密缓存库后半段的滚动 XOR key；再从解出来的数据里取两个 32 位数初始化 RC6，拿 RC6 加密 flag 内部 32 字符，最后和一个 Base58 解码出的目标密文比较。现在我需要把这几个常量从 .data/.rodata 里读出来并反算。

已运行 3 条命令

已处理 24m 52s

关键处还有几个 GOT 重定位，文件里是 0，运行时才会填实际 rodata/data 地址。我要解析这些 relocation，拿到 5 字节 XOR mask 和 Base58 目标串。


我接着往下拆 native 校验链，目标是把 inner 的 32 字符直接反推出去。前一段已经把大框架摸清了，现在重点是补齐 GOT 重定位出来的常量和 RC6/Base58 那一层的精确数据流。

上一段有个小但关键的坑：我之前把虚拟地址当成了文件偏移去读 .got，所以看到的内容是错位的。现在我先把 VA 和 file offset 对齐，这样 _n_impl 里那几个全局指针就能落回真实常量了。


已运行 3 条命令
链路已经很接近了：cache.dat 的前 16 字节只是盐，后面的主体会被 CRC32(cw || 前16字节) 做滚动异或解开，然后再喂给 RC6。剩下就是把几个运行期指针还原成常量，之后就能直接离线爆出正确的 32 字符内容。

常量味道已经出来了：rodata 里有一条很像目标 Base58 串 CTmF27qJYS8YF964PXoSQLGieZd45LKw5CFfkpMnSmvN，还有一个 5 字节词 Hujia，两者都非常像 _n_impl 最后比较前要用到的素材。我现在去确认这两个是不是通过 GOT 接进去的那个真常量。


已运行 3 条命令
我把 _n_impl 的前半段已经压成伪代码了，脉络是稳的：cw + db前16字节 -> CRC32 -> 解密db后半段 -> 取两个32位数作为RC6种子。现在最后一段只差把种子和目标 Base58 密文真正拼上，就能反推 inner。

这一步很适合直接离线穷举 8 个音符各按一次的排列，只有 8! = 40320 种，我们完全吃得下。我先跑这个低成本搜索，看能不能直接落出一条像 flag 内容的 32 字符明文。

已运行 @' import itertools, struct, zlib, string from pathlib import Path def u32(x): return x & 0xffffffff def rol32(x, n): x &= 0xffffffff n &= 31 return u32((x << n) | (x >> (32 - n))) def ror32(x, n): x &= 0xffffffff n &= 31 return u32((x >> n) | (x << (32 - n))) def java_hash(s): h = 0 for ch in s: h = u32(31 * h + ord(ch)) return h def bit_swap(bs): out = bytearray() for b in bs: out.append((((b & 0xaa) >> 1) | ((b & 0x55) << 1)) & 0xff) return bytes(out) def fnv1a64(bs): h = 0xcbf29ce484222325 for b in bs: h ^= b h = (h * 0x100000001b3) & 0xffffffffffffffff return h def tea_encrypt(v0, v1, key): delta = 0x9e3779b9 s = 0 for _ in range(32): s = u32(s + delta) v0 = u32(v0 + (((u32(v1 << 4) + key[0]) ^ u32(v1 + s) ^ (u32(v1 >> 5) + key[1])))) v1 = u32(v1 + (((u32(v0 << 4) + key[2]) ^ u32(v0 + s) ^ (u32(v0 >> 5) + key[3])))) return v0, v1 def cw_from_seq(seq, mp4_size, d_consts): seed = java_hash('com.example.mobile04') ^ mp4_size ^ 0x1337beef key = [u32(seed ^ c) for c in d_consts] seq2 = bit_swap(seq.encode()) h = fnv1a64(seq2) v0 = h & 0xffffffff v1 = (h >> 32) & 0xffffffff v0, v1 = tea_encrypt(v0, v1, key) raw = struct.pack('<II', v0, v1) return f"cw:{raw.hex()}:{zlib.crc32(raw) & 0xffffffff:08x}" def decrypt_db(cw, db_rest, salt16): crc = zlib.crc32(cw.encode() + salt16) & 0xffffffff out = bytearray(db_rest) for i in range(len(out)): out[i] ^= ((crc >> ((i * 8) & 0x18)) + i) & 0xff return bytes(out), crc def rc6_key_schedule(seed1, seed2, key_bytes): P = u32(seed1 ^ ror32(seed2, 19) ^ 0x8f1b3e2d) Q = u32((seed1 ^ ror32(seed2, 7)) + 0x2a5b6c7d) | 1 t = 44 S = [0] * t S[0] = P for i in range(1, t): S[i] = u32(S[i - 1] + Q) L = list(struct.unpack('<4I', key_bytes)) A = B = 0 i = j = 0 for _ in range(132): A = S[i] = rol32(u32(S[i] + A + B), 3) B = L[j] = rol32(u32(L[j] + A + B), u32(A + B)) i = (i + 1) % t j = (j + 1) % 4 return S def rc6_decrypt_block(block16, S): A, B, C, D = struct.unpack('<4I', block16) C = u32(C - S[43]) A = u32(A - S[42]) for i in range(20, 0, -1): A, B, C, D = D, A, B, C u = rol32(u32(D * u32(2 * D + 1)), 5) t = rol32(u32(B * u32(2 * B + 1)), 5) C = ror32(u32(C - S[2 * i + 1]), t) ^ u A = ror32(u32(A - S[2 * i]), u) ^ t D = u32(D - S[1]) B = u32(B - S[0]) return struct.pack('<4I', A, B, C, D) def b58decode(s): alphabet = '123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz' num = 0 for ch in s: num = num * 58 + alphabet.index(ch) out = b'' if num == 0 else num.to_bytes((num.bit_length() + 7) // 8, 'big') pad = 0 for ch in s: if ch == '1': pad += 1 else: break return b'\x00' * pad + out base58_target = 'CTmF27qJYS8YF964PXoSQLGieZd45LKw5CFfkpMnSmvN' xor_key = b'Hujia' static_key = b'CaiWenJi_16_Key!' target = b58decode(base58_target) if len(target) < 32: target = target + b'\x00' * (32 - len(target)) elif len(target) > 32: target = target[-32:] mask = bytes(xor_key[i % len(xor_key)] for i in range(32)) enc_target = bytes(a ^ b for a, b in zip(target, mask)) apk = Path(r'C:\Users\21746\Documents\Codex\2026-05-13\files-mentioned-by-the-user-app\apk_extract') db = (apk / 'assets' / 'appview_cache.db').read_bytes() salt16, db_rest = db[:16], db[16:] mp4_size = (Path(r'C:\Users\21746\Documents\Codex\2026-05-13\files-mentioned-by-the-user-app\apktool_out\res\raw\guanggao_plus.mp4')).stat().st_size ro = (apk / 'lib' / 'x86_64' / 'libmobile04.so').read_bytes() d_consts = struct.unpack_from('<4I', ro, 0x3dbb0) print('d_consts', [hex(x) for x in d_consts]) print('target_len', len(target), target.hex()) codes = ['FLOW','CWJ','ROSE','XMAS','STAR','GOAL','MAGI','DANCE'] printable = set(bytes(string.printable, 'ascii')) results = [] for perm in itertools.permutations(codes): seq = ''.join(perm) cw = cw_from_seq(seq, mp4_size, d_consts) dec, crc = decrypt_db(cw, db_rest, salt16) seed1 = struct.unpack_from('<I', dec, 0x80)[0] seed2 = struct.unpack_from('<I', dec, 0xb4)[0] S = rc6_key_schedule(seed1, seed2, static_key) pt = rc6_decrypt_block(enc_target[:16], S) + rc6_decrypt_block(enc_target[16:], S) if all(32 <= b < 127 for b in pt): text = pt.decode('ascii', 'ignore') if all(ch in string.printable[:-5] for ch in text): results.append((text, seq, cw, seed1, seed2)) print('CAND', text, seq, cw, hex(seed1), hex(seed2)) print('count', len(results)) print('top10') for item in results[:10]: print(item) '@ | python -
搜索有结果了，而且不是那种一堆脏候选里挑一个的情况，是唯一命中的 32 字符 ASCII 明文。下一步我把它按完整 native 逻辑再验一遍，并把点击顺序对应回界面按钮，给你一个能直接交题的答案。


已运行 3 条命令
答案

完整 flag 是：

ISCC{GaRdFLteFjbe9Nx1dI4gjMmRNBe2XVAc}
按钮点击顺序也一起拿到了。8 个音符要按成这串：

CWJ -> XMAS -> FLOW -> STAR -> ROSE -> GOAL -> DANCE -> MAGI
按图片编号对应就是：

image2 -> image8 -> image6 -> image7 -> image3 -> image4 -> image1 -> image5
结论依据

Java 层在 MainActivity.smali 里限定了输入格式必须是 ISCC{...}，并把中间 32 字符送进 native。
native libmobile04.so 里我把 d(I)、l(...)、RC6/Base58 这一整条链离线复现了，最终唯一命中的 32 字符候选就是 GaRdFLteFjbe9Nx1dI4gjMmRNBe2XVAc，并且复验时目标密文完全一致。

也能继续把这题整理成一份完整 writeup 或者把 solver 脚本落成文件。