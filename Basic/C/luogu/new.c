#include<stdio.h>

int main(){
    int n;
    scanf("%d",&n);
    int num;
    while (scanf("%d",&num))
    {
        for (size_t i = 0; i < num/n; i++)
        {
            for (size_t j = 0; j < num&&j<n; i++)
            {
                /* code */
            }
            if (condition)
            {
                /* code */
            }
            
        }
        
    }
    
}